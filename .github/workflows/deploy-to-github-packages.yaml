name: Publish to GitHub Packages
# A push to a branch publishes the snapshot. Renovate and work-in-progress branches are
# left out: they would overwrite the shared snapshot coordinates in the registry, so
# whoever pushed last would decide what the other repositories compile against.
"on":
  push:
    branches-ignore:
      - "renovate/**"
      - "release/**"
      - "feature/wip-**"
concurrency:
  # shared with the pull-request build: one build of this repository at a time, queued
  # and never cancelled, because snapshot artifacts share their coordinates and parallel
  # publishes overwrite each other
  group: businesscockpit-process-engine-api-adapter-build
  cancel-in-progress: false
permissions:
  contents: read
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          java-version: 21
          distribution: temurin
          cache: maven
      - name: Publish package
        run: mvn -s $GITHUB_WORKSPACE/.github/workflows/github-packages-settings.xml
          --batch-mode --no-transfer-progress --update-snapshots deploy
        env:
          USER_NAME: "${{ secrets.VANILLABP_USER_NAME }}"
          USER_TOKEN: "${{ secrets.VANILLABP_USER_TOKEN }}"
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
      - name: Upload the test reports of a failed build
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: test-reports
          path: |
            **/target/surefire-reports/**
            **/target/failsafe-reports/**
            **/target/*application*.log
          retention-days: 7
          if-no-files-found: ignore
